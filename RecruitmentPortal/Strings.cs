namespace RecruitmentPortal;

/// <summary>
/// Used to store hard-coded shared strings
/// </summary>
/// <remarks>
/// Use a resource file instead when localization is needed
/// </remarks>
public struct Strings
{
    public struct Auth
    {
        public struct Roles
        {
            public const string Administrator = "Administrator";
        }
    }
}
